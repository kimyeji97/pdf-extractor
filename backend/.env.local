# AWS 없이 로컬에서 테스트할 때 사용
# cp .env.local .env

STORAGE_BACKEND=local
LOCAL_STORAGE_DIR=./local_storage
LOCAL_BASE_URL=http://localhost:8000

# S3 관련 항목은 로컬 모드에서 사용되지 않으므로 빈 값으로 둬도 됨
AWS_REGION=ap-northeast-2
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
S3_BUCKET_NAME=
