[pytest]
testpaths = tests

# `from app.core.config import settings` 가 해석되려면 backend/ 가 sys.path 에 있어야 한다.
# tests/ 에 __init__.py 가 없으면 pytest 는 tests/ 만 sys.path 에 넣으므로 명시한다.
pythonpath = .

# 케이스 ID 필터: pytest -k 'F09'
