# TODO — 남은 작업과 순서

> 2026-08-28 확정. **위에서 아래 순서로 진행한다.** 각 단계는 착수 시 `/workplan`으로 계획서를 만들고 그때 REQ 번호를 부여한다
> (**다음 번호는 CLAUDE.md "계획 번호 부여" 절이 단일 출처다** — 여기에 옮겨 적지 않는다.
> 2026-08-28에 미러링해 둔 목록이 같은 날 `C09`·`P05`가 소진되며 바로 어긋났다).
> 완료 여부의 단일 출처는 `PROGRESS.md` 인덱스. 여기는 순서표다.

## 작업 순서

> **dev 프론트 배포 정책 (2026-08-28 결정)** — 프론트 변경은 **모아서 한 번에** 올린다.
> 이 로드맵이 끝난 뒤, 또는 **백엔드를 dev에 배포해서 프론트도 같이 올려야만 동작하는 상황**이 오면 그때 배포한다.
> 프론트 배포는 `wrangler` 수동이라(자동 배포 없음) 매 변경마다 돌리는 비용이 크고, dev는 상시 가동이 아니다.
> ⚠️ 그래서 **dev 프론트는 main보다 뒤처져 있는 것이 정상**이다 — "dev에서 안 보인다"를 버그로 읽지 말 것.
> 현재 dev 프론트: `feat/B11-notification-baseline` 빌드(2026-08-28). C09 프론트 변경 미반영.


### 1. 미결 결정 ✅ 2026-08-28 (6건 모두 결정 — 각 계획서 미결 절에 결론 기록)
- [x] F09: 재감지 실패 메시지 출처 → **서버 `message` 단일 출처** (코드 작업은 2단계로)
- [x] F09: 알림 `kind`로 목록 재조회 → **가려 받는다** (코드 작업은 2단계로)
- [x] P04: 콜드 태스크 → **정상 상태 기준, 콜드 제외** (원인 조사는 2단계 "발행 전 6s"와 묶음)
- [x] P04: 전역 딤 측정 → **`GlobalDim` 렌더 여부로** (케이스 추가는 2단계로)
- [x] B11: ready 동승 알림 → **기준선으로 확정(현재 동작)**, 코드 변경 없음
- [x] F09 Phase 6 지연 상한 → **P04 실측으로 해소, ≤ 2s(SSE와 동일)**

### 2. 후속 작업 ✅ 2026-08-28 완료 (완료 REQ에서 파생된 것 + 1단계 결정에서 생긴 코드 작업)
- [x] **REQ-C09** ✅ 2026-08-28 (PR #4 main 머지 `91a911a`) 알림 경로 후속 묶음 — 아래 4건 ([계획서](plans/PLAN-C09-notification-followups.md), 2026-08-28)
  - [x] F09: 재감지·생성 실패 문구를 알림 `message`로 대체 (소)
  - [x] F09: `useNotificationRefresh`에 `kind` 옵션 — 분석 목록 `detection`·생성 이력 `export`만 재조회 (소)
  - [x] P04: 계약 #26 회귀 케이스 — 스트림 이벤트 뒤 `GlobalDim`이 DOM에 없음 (테스트만, 소)
  - [x] P04: `: connected` 선발송 — edge가 첫 바이트까지 헤더를 붙잡아 `EventSource`가 최대 30s CONNECTING (백엔드, 소)
- [x] **REQ-P05** ✅ 2026-08-28 (PR #5 main 머지 `6fba551`) 알림 전달 지연 ([계획서](plans/PLAN-P05-notification-latency.md), 2026-08-28 · 목표 뱃지 ≤ 5s · GET ≤ 1.5s)
  - [x] 감지 완료 알림을 프리워밍 앞으로 — 재감지·최초 감지 두 경로 (중)
  - [x] 첫 진입 피드 GET 3.79s → 건별 읽기 병렬화 (소~중)
  - [x] 콜드 태스크 `onopen` ~31s — 조사 결과 **재현 안 됨**(1004·269·344ms), 원인은 edge 상태로 추정·재현 조건 불명
- [x] P04 파생: 생성 이력 미리보기 URL에 쿼리를 붙여 edge 캐시 키 분리 ✅ 2026-08-28 (PR #6 `dd8c7bc`, 계획서 없이 진행)

### 3. 화면 기능 — D09 → F10 → D10 → 아래 "신규 항목"
- [x] **REQ-D09** 생성 이력 화면 개편 — 목록을 문항 분석과 동일 UI로 + 클릭 시 아코디언 미리보기 (중) — ✅ 2026-09-03
- [x] **REQ-F10** 생성 이력 검색 — `list_workbooks` 서버 검색 선행(페이지네이션, P03-03과 같은 이유) (소~중) — ✅ 2026-09-03
- [x] **REQ-D10** 문항 목록 n×n 바둑판 배열 — 임계 420px 초과 시에만 열 수 증가(D01과 공존, [계획서](plans/PLAN-D10-question-grid-columns.md) 배경 절에 명시) (소) — ✅ 2026-09-03 (PR #8 main 머지 `48e445d`. 오탐 배지 육안은 다음 dev 배포 후)
- [ ] 신규 항목 (아래 "신규 항목" 절, 나열 순서대로)

### 4. 서버 영역 버그 (문항 추출 여백)
- [ ] 문항 추출 시 하단 여백 불일치
- [ ] 문항 추출 시 좌/우/상 여백 통일

### 5. REQ-27 로그인/회원가입 (+ D07 마무리)
- [ ] **REQ-27** — 인증 전무·CORS `*`. 사용자·세션·보호 라우트·스토리지 사용자 분리·기존 데이터 귀속. 착수 전 `/workplan`에서 분리·귀속 방식부터 결정 (대)
- [ ] REQ-D07 잔여 — `auth-layout`·`account-popover` 슬롯 연결 → 27과 함께 닫음
- [ ] CORS 제한 (CLAUDE.md 상시 이슈)
- ⏸ REQ-28 공유 — 27 뒤에 별도 판단

### 6. 운영 환경 구성
- [ ] **밀린 dev 프론트 재배포** (위 배포 정책 — 로드맵 완료 시점 또는 백엔드 배포에 딸려서)
- [ ] prod 환경 분리 (현재 dev 하나 — 백엔드 desired 0 토글 운영, 프론트 wrangler 수동 배포)
- [ ] 프론트 자동 배포 / 백엔드 배포 파이프라인
- [ ] 테스트 커버리지 — 알림 경로 외 0건(문항 감지·PDF 생성)
- [ ] F09 Phase 6 브라우저 알림 (런칭 준비 시점, 08-06 결정)
- ❌ REQ-E01 감지 진행률 스트리밍 — 차후, P04(SSE) 위에 재검토 가능

---

## 신규 항목 (2026-08-28 사용자 작성 — 3단계 마지막에 이 순서로)

[ ] 메뉴 이름 변경
  - 분석 (as-is 문항 분석)
  - 생성 (as-is: 문제집 생성)
  - 결과 (as-is: 생성 이력)
  - 템플릿 관리 (현재는 표지만) (as-is: 표지 관리?)

(아래는 as-is, to-be 메뉴 이름 섞여 있음.)

[ ] 여기서 제공하는 테마가 있으면 좋을 듯.
  - ex. 컬러 테마, 문항 글씨체 등.. 디자인 등.., 머릿글/바닥글 쪽 디자인 등..
[ ] 표지 등록하는 것 처럼 각주나 워터마크도 등록하면 좋을 듯.
[ ] 템플릿 관리에서 표지,각주,워터마크 등 등록된 걸 조합해서 개인만의 템플릿으로 만들고
  - 이걸 문제집 생성에서 사용할 수 있게 하면 좋을 듯.
  - 지금은 문제집 생성에서 '표지'를 선택하는데, '템플릿'을 선택하는 거지
[x] 좌측 상단에 '문제집 검색' 입력란 제거 — ✅ REQ-D09 Phase 4로 완료(2026-09-03)
[ ] 벨 알림을 클릭하면, 분석으로 넘어가는데 문서 이름이 관리명이 아님. (job-id 인 듯)
[ ] 문항분석 현황판 정보 변경
  - 오탐지 문항 개수 (클릭: 오탐지 문항이 존재하는 페이지 리스트가 나오고 클릭하면 이동)
  - 수동 설정 문항 개수 (클릭: 수동 문항이 존재하는 페이지 리스트가 나오고 클릭하면 이동)
  - 분석 정확도 (전체 문항수 - (오탐지 문항수+ 수동 문항수)의 비율)