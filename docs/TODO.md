# TODO — 남은 작업과 순서

> 2026-08-28 확정. **위에서 아래 순서로 진행한다.** 각 단계는 착수 시 `/workplan`으로 계획서를 만들고 그때 REQ 번호를 부여한다
> (다음 번호는 CLAUDE.md "계획 번호 부여" 참고 — 2026-08-28 기준 `B12`·`C09`·`D11`·`F12`·`P05`·`29`).
> 완료 여부의 단일 출처는 `PROGRESS.md` 인덱스. 여기는 순서표다.

## 작업 순서

### 1. 미결 결정 ✅ 2026-08-28 (6건 모두 결정 — 각 계획서 미결 절에 결론 기록)
- [x] F09: 재감지 실패 메시지 출처 → **서버 `message` 단일 출처** (코드 작업은 2단계로)
- [x] F09: 알림 `kind`로 목록 재조회 → **가려 받는다** (코드 작업은 2단계로)
- [x] P04: 콜드 태스크 → **정상 상태 기준, 콜드 제외** (원인 조사는 2단계 "발행 전 6s"와 묶음)
- [x] P04: 전역 딤 측정 → **`GlobalDim` 렌더 여부로** (케이스 추가는 2단계로)
- [x] B11: ready 동승 알림 → **기준선으로 확정(현재 동작)**, 코드 변경 없음
- [x] F09 Phase 6 지연 상한 → **P04 실측으로 해소, ≤ 2s(SSE와 동일)**

### 2. 후속 작업 (완료 REQ에서 파생된 것 + 1단계 결정에서 생긴 코드 작업)
- [ ] F09: 재감지 실패 문구를 알림 `message`로 대체 (`work.jsx` 자체 문자열 제거, 소)
- [ ] F09: `useNotificationRefresh`에 `kind` 옵션 — 분석 목록 `detection`·생성 이력 `export`만 재조회 (소)
- [ ] P04: 계약 #26 회귀 케이스 — 스트림 이벤트 뒤 `GlobalDim`이 DOM에 없음 (테스트만, 소)
- [ ] P04: `: connected` 선발송 — edge가 첫 바이트까지 헤더를 붙잡아 `EventSource`가 최대 30s CONNECTING (백엔드, 소)
- [ ] P04: 재감지 완료 → 발행 전 서버 내부 저장 ~6s 단축 (활성 탭 벨 9~12s의 대부분, 중)
- [ ] P04: 첫 진입 알림 GET 3.8s (소~중)
- [ ] P04 파생: 생성 이력 미리보기 URL에 쿼리를 붙여 다운로드 링크와 edge 캐시 키 분리 (R2 CORS 재발 방지, 소)

### 3. 화면 기능 — D09 → F10 → D10 → 아래 "신규 항목"
- [ ] **REQ-D09** 생성 이력 화면 개편 — 목록을 문항 분석과 동일 UI로 + 클릭 시 아코디언 미리보기 (중)
- [ ] **REQ-F10** 생성 이력 검색 — `list_workbooks` 서버 검색 선행(페이지네이션, P03-03과 같은 이유) (소~중)
- [ ] **REQ-D10** 문항 목록 n×n 바둑판 배열 — 임계 너비 초과 시에만 열 수 증가(D01과 방향 반대, 스펙에 명시) (소~중)
- [ ] 신규 항목 (아래 "신규 항목" 절, 나열 순서대로)

### 4. 서버 영역 버그 (문항 추출 여백)
- [ ] 문항 추출 시 하단 여백 불일치
- [ ] 문항 추출 시 좌/우/상 여백 통일

### 5. REQ-27 로그인/회원가입 (+ D07 마무리)
- [ ] **REQ-27** — 인증 전무·CORS `*`. 사용자·세션·보호 라우트·스토리지 사용자 분리·기존 데이터 귀속. 착수 전 `/workplan`에서 분리·귀속 방식부터 결정 (대)
- [ ] REQ-D07 잔여 — `auth-layout`·`account-popover` 슬롯 연결 → 27과 함께 닫음
- [ ] CORS 제한 (CLAUDE.md 상시 이슈)
- ⏸ REQ-28 공유 — 27 뒤에 별도 판단

### 6. 운영 환경 구성
- [ ] prod 환경 분리 (현재 dev 하나 — 백엔드 desired 0 토글 운영, 프론트 wrangler 수동 배포)
- [ ] 프론트 자동 배포 / 백엔드 배포 파이프라인
- [ ] 테스트 커버리지 — 알림 경로 외 0건(문항 감지·PDF 생성)
- [ ] F09 Phase 6 브라우저 알림 (런칭 준비 시점, 08-06 결정)
- ❌ REQ-E01 감지 진행률 스트리밍 — 차후, P04(SSE) 위에 재검토 가능

---

## 신규 항목 (2026-08-28 사용자 작성 — 3단계 마지막에 이 순서로)

[ ] 메뉴 이름 변경
  - 분석 (as-is 문항 분석)
  - 생성 (as-is: 문제집 생성)
  - 결과 (as-is: 생성 이력)
  - 템플릿 관리 (현재는 표지만) (as-is: 표지 관리?)

(아래는 as-is, to-be 메뉴 이름 섞여 있음.)

[ ] 여기서 제공하는 테마가 있으면 좋을 듯.
  - ex. 컬러 테마, 문항 글씨체 등.. 디자인 등.., 머릿글/바닥글 쪽 디자인 등..
[ ] 표지 등록하는 것 처럼 각주나 워터마크도 등록하면 좋을 듯.
[ ] 템플릿 관리에서 표지,각주,워터마크 등 등록된 걸 조합해서 개인만의 템플릿으로 만들고
  - 이걸 문제집 생성에서 사용할 수 있게 하면 좋을 듯.
  - 지금은 문제집 생성에서 '표지'를 선택하는데, '템플릿'을 선택하는 거지
[ ] 좌측 상단에 '문제집 검색' 입력란 제거
[ ] 벨 알림을 클릭하면, 분석으로 넘어가는데 문서 이름이 관리명이 아님. (job-id 인 듯)
[ ] 문항분석 현황판 정보 변경
  - 오탐지 문항 개수 (클릭: 오탐지 문항이 존재하는 페이지 리스트가 나오고 클릭하면 이동)
  - 수동 설정 문항 개수 (클릭: 수동 문항이 존재하는 페이지 리스트가 나오고 클릭하면 이동)
  - 분석 정확도 (전체 문항수 - (오탐지 문항수+ 수동 문항수)의 비율)