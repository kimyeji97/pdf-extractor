# PLAN-B10 · 문제집 생성 중 화면 이탈 시 메타·다운로드 유실

> 출처: 2026-07-30 세션 (REQ-F09 계획 점검 중 파생) · 작성: 2026-07-30
> 상태: ✅ **코드 완료 (2026-07-31)** — Phase 0~4 전부. 미결 0건.
> ⏸ **배포 보류 (2026-08-03 사용자 결정)** — 순서는 **백엔드 → 프론트 고정**.
> 배포 전까지 운영(dev)에서 고아는 계속 생기므로 7-31 청소 결과는 시간이 지나면 낡는다.
> 남은 미검증 2건: 편집 화면 버튼부터 도는 UI 왕복(브라우저 확장 미연결), 메타 저장 실패 분기.

## 배경

문제집 생성을 걸어 둔 뒤 편집 화면을 떠나면 **PDF는 서버에 만들어지지만 생성 이력에는 영원히
나타나지 않는다.** 사용자 입장에서는 "생성이 실패했다"로 보이고, 실제로는 결과물이
`results/{job_id}/`에 남아 아무도 참조하지 않는 고아가 된다.

원인은 문제집 메타를 만드는 유일한 주체가 **프론트 폴링의 `DONE` 분기**라는 것이다
(`extract.py`에 `workbook` 언급 0건 — 백엔드는 메타를 만들지 않는다). 그 폴링은
언마운트에서 `clearInterval`로 죽으므로, 화면을 떠나면 `createWorkbookMeta()`가 영원히
호출되지 않는다. 자동 다운로드(`a.click()`)도 같은 분기 안에 있어 함께 사라진다.

**생성 이력 화면은 `workbooks/`만 읽는다** — 메타가 없으면 결과 PDF가 존재해도 목록에 없다.

> 이 버그는 REQ-F09(완료 알림) 계획을 점검하다 코드 흐름으로 발견했다. F09가 폴링을 앱 셸로
> 올리면 자연히 함께 고쳐지지만, **불편(알림 없음)과 데이터 손실은 급이 다르므로 분리해
> 먼저 고친다**(2026-07-30 사용자 결정).

## 범위

**포함**

- 생성 중 화면 이탈·새로고침·탭 종료 시에도 문제집 메타가 남게 한다
- 위 상황에서 유실되는 자동 다운로드 처리
- `createWorkbookMeta` 실패를 침묵으로 삼키는 `catch {}` (editor/index.jsx) 정리

**제외**

- **알림 표면(벨·스낵바·브라우저 알림) 일체** — REQ-F09 소관. B10은 "결과가 남는가"만 책임진다
- **전역 폴링 인프라 신설** — F09가 만든다. B10이 그걸 기다리면 "먼저 고친다"는 결정이 무의미해지므로,
  B10은 F09 인프라에 의존하지 않는 해법이어야 한다
- **문항 감지(`boundaries_status`) 경로** — 이쪽은 결과가 job에 귀속돼 유실이 없다. 재감지 폴링이
  죽어도 다음 조회 때 상태가 보인다

## 결정

| 항목 | 결정 | 근거 | 기각한 안 |
|------|------|------|-----------|
| F09와의 관계 | **분리해 B10을 먼저 고친다** | 데이터 손실이 알림 부재보다 급이 높다. F09는 규모가 크고 미결이 남아 있어 그 뒤에 손실을 방치하게 된다 | F09 전역 폴링에 흡수시키기 — 손실 해소가 F09 완료 시점까지 밀린다 |
| 해법 방향 | **안 A′ — 백엔드가 생성 성공 시 메타를 쓴다** | 프론트 수명과 완전히 분리된다. 폴링이 죽든 탭이 닫히든 무관 | **안 B**(프론트 선저장) — 백엔드 무변경이지만 저장 호출 실패 창이 남고 갱신이 여전히 폴링 의존이라 `PROCESSING`에 멈춘 메타가 생긴다. **안 C**(이력이 export job 직접 조회) — 유실을 막지 않고 표시로 덮는 것이라 이름·선택 목록이 복구되지 않아 편집 복원이 안 된다 |
| 저장 시점 | **성공 시에만 쓴다** | 요청 시점에 `PROCESSING`으로 선저장하면 이력에 미완성·실패 항목이 노출되고 상태 필드·표시가 따라붙는다 | 요청 시점 선저장 |
| 자동 다운로드 | **편집 화면에 머문 경우로 한정** | 화면을 떠난 사용자에게 브라우저 다운로드를 강제할 수단이 없다. 이탈했으면 생성 이력에서 받아 간다 | 복귀 시 재시도 — 의도치 않은 시점에 다운로드가 시작되고 F09 알림과 역할이 겹친다 |
| 고아 복구 | **Phase 0에서 건수를 세고 판단** | 로컬에 `local_storage`가 없어 실데이터는 R2 dev에만 있다. 0건이면 복구 작업 자체가 불필요 | 무조건 복구 / 무조건 미복구 — 둘 다 건수를 모르고 정하는 것 |

> **안 A′가 예상보다 싸다.** `workbook.py` docstring은 프론트 주도 저장을 택한 이유를
> "extract-v2는 PDF만 생성하고 selections/layout 정보를 모르기 때문"이라 적어 뒀는데,
> **그 근거는 현재 사실이 아니다** — `ExtractV2Request`가 `selections`와 `layout`을 둘 다 받는다.
> 실제 격차는 3개뿐이다: 문제집 이름(`filename`/`name`, 같은 값이 들어간다), 선택 항목의
> `workbook_name`·`source_filename`. `title`은 이미 `label`로 같은 값이 넘어온다.
> **이 docstring은 수정 시 함께 고쳐야 한다** — 안 고치면 다음 사람이 같은 근거로 되돌린다.

## 미결 질문

- [x] 해법 방향 · 저장 시점 · 자동 다운로드 범위 — 2026-07-30 확정 (결정 표 참조)
- [x] **고아 3건 처리 — 삭제**(2026-07-31). 사용자가 내용을 기억하지 못해 **복구는 기각**했다.
      이름·선택 목록이 없어 편집 복원이 안 되는데 내용도 모르면 "정체를 모르는 항목"만 3개 는다.

## 작업 단계

- [x] **Phase 0 — 재현 확정** *(2026-07-31 완료)*
      완료 기준 3개 **전부 성립**. 재현은 브라우저 대신 **API 레벨**로 했다 — 생성 요청 후
      `createWorkbookMeta`를 호출하지 않는 것이 "화면을 떠난 프론트"와 정확히 같은 상태이고,
      브라우저 타이밍에 의존하지 않아 더 결정적이다.
      - ① 결과 PDF 존재: `1,986,299 bytes` 생성됨
      - ② workbook 메타 없음: 1건 → 1건 (증가 없음)
      - ③ 이력 목록 미표시: 총 1건, 해당 export job 없음
      - **대조군**: 같은 export job에 `createWorkbookMeta`만 호출하니 즉시 이력에 등재(1 → 2건).
        **이 호출 하나의 유무로 등재 여부가 갈린다**는 것이 직접 확인됐다.

      **고아 실측 — R2 dev에서 3건.** EXPORT job 14건 중 메타 없는 것 3건(**21%**), 전부
      `status=DONE`이고 결과 PDF가 실재한다(1.9MB / 246KB / 2.6MB). **그중 1건은 2026-07-29
      생성** — 과거에 끝난 일이 아니라 현재 진행형이다.
      | export job | 크기 | 생성일 |
      |---|---|---|
      | `42173649…` | 1,949,396 B | 2026-07-20 |
      | `6d6e1bfb…` | 246,798 B | 2026-07-20 |
      | `9ea99e6d…` | 2,595,506 B | 2026-07-29 |

      > **정정** — 계획 작성 시 "로컬에 `local_storage`가 없어 R2에서만 셀 수 있다"고 적었으나
      > 실제 위치는 `backend/local_storage`였다(레포 루트에서 찾아 놓친 것). 다만 로컬 데이터는
      > 1건뿐이라 표본 가치가 없고, **`backend/.env`가 `STORAGE_BACKEND=s3`로 R2 dev를 가리키므로**
      > 실측은 R2 실데이터로 했다.

- [x] **Phase 1 — 스키마 확장 + `scale` 전송 누락 수정** *(2026-07-31 완료)*
      `ExtractV2Request.workbook_name`(만들 문제집 이름), `SelectionItem.workbook_name`(**출처**
      문제집 이름)·`source_filename` 추가. 셋 다 `Optional`.
      - ① 구형 페이로드(3필드 없음) → **HTTP 200**, 생성 DONE
      - ② 신형 페이로드(3필드 전부) → **HTTP 200**, 생성 DONE
      - ③ 대조군: ①과 ②의 결과 PDF **1페이지 픽셀 해시 동일** — 신규 필드가 렌더에 영향 없음
      - ④ 메타 저장 없음: 1건 → 1건 (Phase 2 미구현이므로 정상)

      **`workbook_name`의 유무가 "메타를 누가 저장하는가"의 신호다**(2026-07-31 결정).
      있으면 새 프론트로 보고 백엔드가 저장하고, 없으면 구 프론트로 보고 저장하지 않는다.
      이 분기 덕에 **과도기 이중 저장 창이 사라진다** — 백엔드를 먼저 배포해도 중복도, 이름
      없는 메타도 생기지 않는다. 값이 아니라 **존재 여부**가 의미를 가지므로 기본값을 채우면 안 된다.

      **끼워 고친 별건 — `scale`이 생성 PDF에 반영되지 않았다.** `client.js`의 `startExtractV2`
      전송 맵에 `scale`이 빠져 있었다(백엔드는 `pdf_service._sel_scale()`로 실제 사용 중).
      저장(`POST /api/workbooks`)에는 들어가서 미리보기·편집 복원은 맞았기 때문에
      **"결과 PDF만 다르다"로만 나타났다.** `예정된작업.md`에 ✅로 적힌 항목이 반쪽만 동작한 것.
      검증: scale 1.0 vs 1.6의 결과 PDF **픽셀 해시가 다름**(백엔드가 값을 실제로 반영).
      ⚠️ **파일 크기는 지표가 안 된다** — 원본 페이지를 벡터로 임베드해 크기가 지배적으로 같다
      (실측 1.0과 1.6이 **둘 다 1,986,288 bytes**). 한 번 "미반영"으로 오판했다. 픽셀로 볼 것.
      프론트 수정분은 빌드 통과까지만 확인했고 **브라우저 왕복은 Phase 3에서 함께 본다.**

- [x] **Phase 2 — 백엔드 저장 이관** *(2026-07-31 완료)*
      `_process_extraction_v2`가 생성 성공 직후 `_save_workbook_meta()`를 호출한다.
      `workbook.py`의 낡은 docstring도 정정했다.
      **Phase 0의 재현 절차를 그대로 반복해 검증했다** — 프론트가 `createWorkbookMeta`를
      호출하지 않는 상황(=화면 이탈·새로고침·탭 종료를 모두 포괄)에서:
      - ① `workbook_name` **있음** → 메타 1→2건, **이력 목록에 표시됨**. `name`/`filename`/`layout`/
        `question_count`/`result_job_id` 정확, selection의 `title`·출처 2필드·`scale`까지 매핑 확인
      - ② `workbook_name` **없음**(구 프론트) → 저장하지 않음. 종전 경로 보존 확인
      - ③ 생성 **실패**(FAILED) → 저장하지 않음. 이력에 미완성 항목이 안 생긴다
      - ④ 단건 조회(편집 복원 경로) → selections 2건 온전, 필수 필드 누락 없음

      **메타 저장 실패는 생성 실패로 둔갑시키지 않는다** — PDF는 이미 만들어졌으므로 상태는
      `DONE`으로 두고 `error`에 사유만 남긴다. 이 분기는 유도가 어려워 **미검증**으로 남겼다.

- [x] **Phase 3 — 프론트 정리** *(2026-07-31 완료)*
      신규 필드 전송과 `createWorkbookMeta` 호출 제거를 **한 커밋에** 넣었다(원자성 제약).
      `catch {}`도 함께 사라졌고, `createWorkbookMeta` import도 정리했다.
      - **실제 모듈이 만드는 페이로드를 브라우저에서 캡처해 확인**(headless Chrome/CDP,
        페이지 컨텍스트에서 `import('/src/api/client.js')` 후 `fetch` 가로채기):
        최상위 `workbook_name` · 선택별 `workbook_name`/`source_filename` · `scale`(1.4, 1)
        전부 실림. 요청은 `extract-v2` **1건뿐** — `POST /api/workbooks` 없음.
      - 그 페이로드 형태를 그대로 백엔드에 넣은 왕복: 메타 **신규 1건**(2건이면 이중 저장),
        이력 등재 1건, `scale` 1.4 보존, 출처 2필드 보존.
      - `editor/index.jsx`: `trimmed`가 같은 스코프에서 4번째 인자로 전달됨을 확인.
      - 프론트 빌드 통과.

      > 브라우저 확장(claude-in-chrome)이 연결돼 있지 않아 **UI를 클릭해 도는 왕복은 못 했다.**
      > 대신 실제 모듈을 브라우저 런타임에서 실행해 페이로드를 잡는 방식으로 대체했다 —
      > 편집 화면의 버튼 클릭부터 도는 육안 확인은 남아 있다.

- [x] **Phase 4 — 고아 데이터 처리** *(2026-07-31 완료 — 삭제)*
      복구는 기각(사용자가 내용을 기억하지 못함 → 되살려도 정체 모를 항목만 3개 는다).
      **손으로 R2 키를 고르지 않고 앱 자신의 `DELETE /api/jobs/{id}`를 썼다** — 무엇이
      "연관 저장물"인지는 앱이 알고 있고, 수작업으로 고르면 빠뜨리거나 더 지운다.
      삭제 직전 참조 여부를 한 번 더 확인하고 진행. 3건 모두 HTTP 204.
      - EXPORT job **14 → 11**, 문제집 메타 11 → 11, **고아 3 → 0**
      - R2 잔존 오브젝트 0개 (6개 오브젝트 · 4,792,912 bytes 회수)
      - 남은 문제집 11건 전부 결과 PDF 존재 — **다른 데이터를 건드리지 않았다**

## 제약·함정

- **`workbook_name`은 고유하지 않다**(계약 #17). 고아 복구나 중복 판정을 이름으로 하면 안 된다 —
  실데이터에 같은 이름 2건이 있다. 키는 `job_id`.
- **폴링의 `DONE` 분기에 부수효과를 두지 말 것** — 이 버그의 본질이다. 그 분기는 화면 수명에
  묶여 있어 "화면이 살아 있는 동안만 일어나는 일"만 넣어야 한다(UI 상태 전환). 영속 저장은 안 된다.
  → B10 완료 후 **`CLAUDE.md` 계약으로 승격 제안**(5절 참조).
- **`extract-v2`는 이미 배포된 API다.** 추가 필드는 전부 `Optional`이고, **백엔드를 먼저 배포한 뒤
  프론트를 내보낸다**(Phase 2 → 3).
- **~~이중 저장 주의~~ — Phase 1의 "유무 신호" 설계로 해소됐다.** 백엔드는 `workbook_name`이
  실려 온 요청에만 저장하므로, 프론트 배포 전에는 종전대로 프론트만 저장한다. 대신 **프론트
  배포는 원자적이어야 한다** — 필드 전송과 `createWorkbookMeta` 제거가 같은 배포에 들어가야 한다.
  둘이 갈리면(필드는 보내는데 호출이 남음) 그때 이력에 2건이 뜬다.
- **PDF 비교에 파일 크기를 쓰지 말 것** — 원본 페이지를 벡터로 임베드하므로 배율이 달라도
  크기는 거의 같다(실측: scale 1.0과 1.6이 **바이트까지 동일**). 해시도 생성 시각이 박혀
  무의미하다. **1페이지를 렌더해 픽셀로 비교**해야 한다.
