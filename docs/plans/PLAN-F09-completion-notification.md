# PLAN-F09 · 문항 분석·문제집 생성 완료 알림

> 출처: 2026-07-30 세션 (D07 스펙 §6 제안분 착수) · 작성: 2026-07-30
> 상태: 📝 **초안 — 미결 5건, REQ-B10 선행** (표면 설계 전 미결 해소 필요)

## 배경

오래 걸리는 작업 두 가지가 **끝났는지 알 방법이 없다.**

- **문항 감지** — 문항 분석 목록 화면에 폴링이 아예 없다. 업로드 후 `fetchJobs()`를 한 번 부르고
  끝이라, "분석 중" 배지가 뜬 카드는 새로고침 전까지 영원히 그 상태다. `isAnalyzing()`이 클릭까지
  막으므로 사용자는 **기다리는 것 말고 할 수 있는 게 없는데 언제 끝나는지는 모른다.**
- **문제집 생성** — 폴링은 있으나 편집 화면에 묶여 있다(언마운트에서 `clearInterval`).
  화면을 떠나면 감시가 죽는다.

D07 스펙 §6은 이 작업을 "폴링은 이미 있으니 **알림 표면만 추가**, 규모 소~중"으로 추정했다.
**이 추정은 절반만 맞다.** 알림의 존재 이유가 "다른 일을 하는 동안 알려주는 것"인데 두 폴링 모두
화면 수명에 묶여 있어, 표면만 붙이면 **화면을 떠난 순간 알림도 함께 죽는다.**
따라서 이 작업의 실제 무게중심은 벨 아이콘이 아니라 **폴링을 앱 셸로 올리는 쪽**에 있다.

헤더 우측 슬롯은 D07에서 이미 비워 뒀다(`layouts/dashboard/layout.tsx:108`).

## 범위

**포함**

- 진행 중 작업 레지스트리 + 앱 셸 레벨 전역 폴링
- 헤더 알림 벨 + 미읽음 뱃지 + **알림 이력 팝오버 목록**
- 완료 순간의 인앱 스낵바 (클릭 시 해당 화면으로 이동)
- **브라우저 알림(Web Notification API)** — 탭을 떠나 있어도 알린다
- 문항 분석 목록·생성 이력 화면이 완료를 자동 반영 (새로고침 불필요)

**제외**

- **문제집 메타 유실 버그** — REQ-B10으로 분리해 **먼저** 고친다(2026-07-30 결정).
  F09의 전역 폴링이 자연히 함께 고치지만, 데이터 손실을 F09 완료까지 방치할 수 없다는 판단.
- **서버 푸시(SSE/WebSocket)** — 백엔드는 이미 `boundaries_status`와 `/api/status/{job_id}`를
  노출한다. 폴링으로 충분하고, 푸시로 가면 백엔드 변경 + 커넥션 관리가 붙어 규모가 완전히 달라진다.
- **알림의 사용자별 분리** — 인증이 없다(REQ-27). 누구의 작업인지 서버가 모르므로 알림도
  브라우저 로컬 범위를 넘을 수 없다.
- **인앱 스낵바만 만드는 축소안** — 아래 결정 참조.

## 결정

| 항목 | 결정 | 근거 | 기각한 안 |
|------|------|------|-----------|
| 알림 표면 범위 | **헤더 벨 + 미읽음 뱃지 + 이력 목록까지** | 스낵바는 그 순간 화면을 보고 있어야만 유효하다. 자리를 비운 사이 끝난 작업을 나중에 확인할 수단이 필요하다 | 스낵바만 — 놓치면 복구 불가라 "완료 알림"의 목적을 절반만 달성 |
| 브라우저 알림 | **포함 — 탭을 떠나 있어도 알린다** | 감지·생성이 오래 걸려 사용자가 다른 탭으로 넘어가는 것이 정상 사용 패턴 | 인앱 전용 — 탭 전환 시 무용 |
| B10과의 관계 | **B10을 분리해 먼저 고친다** | 데이터 손실 > 알림 부재 | F09에 흡수 — 손실 해소가 F09 완료 시점까지 밀린다 |
| 폴링 대상 | **진행 중 job을 개별 폴링** (`getJobInfo` / `getStatus`) | `/api/jobs`는 페이지네이션 + 통계가 얹혀 무겁고, 계약 #15("상태 얻자고 목록 API 부르지 말 것")와 어긋난다 | 목록 API 주기 폴링 |

## 미결 질문

- [ ] **알림 이력을 어디에 보관하나.** 백엔드에 알림 저장소가 없다. localStorage로 가면 기기·브라우저를
      넘지 못하고 시크릿 창에서 사라진다. 서버 저장으로 가면 인증 부재(REQ-27) 때문에
      "누구의 알림인지"를 정의할 수 없다. **보관 기간·개수 상한도 함께 정해야 한다.**
- [ ] **알림 대상 범위.** "이 브라우저에서 내가 시작한 작업"만인가, 아니면 조회 시점에 서버에서
      진행 중인 모든 job인가. 후자면 새로고침·다른 탭에서 시작한 작업도 잡히지만, 인증이 없어
      **남의 작업까지 알리게 된다.**
- [ ] **브라우저 알림 권한을 언제 요청하나.** 앱 진입 즉시 요청하면 맥락 없는 권한 팝업이 되고,
      첫 작업 시작 시 요청하면 그 작업의 알림을 놓칠 수 있다.
- [ ] **권한이 거부됐을 때.** 헤더 벨 + 스낵바만으로 충분하다고 볼지, 거부 상태를 사용자에게
      알릴지(알린다면 어디서).
- [ ] **읽음 처리 방식.** 팝오버를 여는 순간 전체 읽음인지, 항목별인지.

## 작업 단계

- [ ] **Phase 0 — B10 선행 확인**
      완료 기준: PLAN-B10 Phase 3까지 완료(백엔드 저장 이관 + 프론트 정리). B10을 먼저 고치기로
      했으므로 F09 착수의 전제다. B10이 프론트의 `createWorkbookMeta` 호출을 걷어내므로,
      **F09 Phase 2에서 이관할 생성 폴링은 순수 UI 상태 전환만 남은 상태**여야 한다.

- [ ] **Phase 1 — 진행 중 작업 레지스트리 + 전역 폴링**
      앱 셸 레벨에 신설한다. **`providers/` 디렉토리는 D07 Phase 1에서 사라졌다**(CLAUDE.md의
      디렉토리 구조가 낡았다) — 둘 자리를 새로 정해야 한다.
      완료 기준: 감지·생성을 시작한 뒤 **다른 라우트로 이동해도** 상태 전이가 감지된다.
      레지스트리가 비면 폴링이 멈춘다(네트워크 탭에서 요청 중단 확인).

- [ ] **Phase 2 — 화면 지역 폴링 2개 이관**
      `pages/analysis/work.jsx:154`(재감지), `pages/editor/index.jsx:247`(생성)를 걷어내고
      레지스트리에 등록하는 방식으로 바꾼다.
      완료 기준: 두 화면의 기존 UI 동작(진행 표시·실패 메시지·완료 후 갱신)이 그대로 유지되고,
      화면에 남아 있는 경우의 사용자 경험이 이전과 동일하다.

- [ ] **Phase 3 — 헤더 벨 + 팝오버 + 스낵바**
      완료 기준: 완료 시 스낵바가 뜨고, 벨에 미읽음 수가 표시되며, 팝오버에서 항목 클릭 시
      해당 화면으로 이동한다. **라이트/다크 양쪽에서 확인**(아래 함정 참조).

- [ ] **Phase 4 — 브라우저 알림**
      완료 기준: 탭을 백그라운드로 두거나 다른 앱으로 전환한 상태에서 완료 시 OS 알림이 뜬다.
      권한 거부 시 인앱 경로가 정상 동작한다.

- [ ] **Phase 5 — 목록 화면 자동 반영**
      완료 기준: 문항 분석 목록의 "분석 중" 배지가 새로고침 없이 해제되고 카드가 클릭 가능해진다.
      생성 이력에 새 문제집이 새로고침 없이 나타난다.

## 제약·함정

- **색조 배경은 `theme/tint.js`의 `tintBg`/`tintSx`/`tintFg`를 쓴다**(계약 #20). 미읽음 뱃지·강조
  배경은 이 계약이 정확히 겨냥하는 자리다 — `primary.lighter`는 **정상 토큰인데 다크에서 깨지고**,
  하드코딩이 아니라 grep에 안 잡히며 콘솔·빌드도 조용하다.
  ⚠️ `tintSx`/`tintFg`는 **함수를 반환한다.** 객체 sx에 스프레드하면 아무것도 안 들어가고 에러도 안 난다.
- **raw `var(...)` 대신 sx 토큰**(계약 #18). 접두사는 `--palette-*`이며 틀린 이름은 에러 없이
  상속색으로 떨어진다. 같은 실패를 이 프로젝트에서 이미 두 번 밟았다.
- **헤더를 건드리면 높이 체인을 확인한다**(계약 #1). 이 앱은 100dvh 작업대라 `flex:1`/`minHeight:0`/
  `overflow:hidden`이 한 군데만 빠져도 내부 패널 스크롤이 죽는다.
- **비활성 탭의 `setInterval`은 브라우저가 조인다**(백그라운드 throttling). 탭을 떠나 있어도 알린다는
  결정과 정면으로 부딪치는 지점이다. 실측으로 지연 폭을 확인하고, 필요하면 다른 타이머 수단을
  검토해야 한다. **현재 미확인.**
- **브라우저 알림은 보안 컨텍스트를 요구한다** — dev(Cloudflare Pages HTTPS)와 로컬(localhost)
  모두 충족하나, 다른 경로로 접근하는 경우가 생기면 조용히 동작하지 않는다.
- **검증은 수치 + 육안을 함께 간다.** Phase 3-4·3-5에서 두 번 확인된 교훈으로, 스크롤·개수·콘솔
  에러가 전부 통과인데 스크린샷을 눈으로 보고서야 잡힌 결함이 있었다. headless Chrome/CDP
  드라이버는 스크래치패드에 만들어 둔 방식을 재사용한다(레포에 puppeteer 의존성 없음).
