# PLAN-P04 · 상시 폴링 → 서버 푸시 전환

> 출처: 2026-08-03 세션(REQ-F09 미결 검토에서 파생) + 2026-08-10 세션 · 작성: 2026-08-10 · 상태: 🟡 진행 (Phase 0 실측 완료 2026-08-18 · 미결 4건 확정 + Phase 1~3 정의 2026-08-26 · **Phase 1·2 완료 2026-08-27** · Phase 3 dev 배포 + CLI 실측 2026-08-27, 브라우저 실측·desired 0 남음)

## 배경

**F09가 "모두의 알림"을 택하면서 폴링이 상시화됐다.** 내가 시작한 작업이 없어도 남이 시작한
게 끝날 수 있어 조건부 폴링이 성립하지 않는다 — 앱이 떠 있는 내내 5초(숨김 30초)마다
`GET /api/notifications`가 돈다.

**이 전환은 성능 개선이 아니라 조건 이행이다.** 2026-08-03에 사용자가 상시 폴링을 승인하며
붙인 조건이 *"차후 상시 폴링 하는 부분 모두 웹소켓으로 변경"*이었다. 그 약속의 이행분이다.

폴링으로는 못 푸는 것이 셋 있다:

- **비활성 탭 타이머 스로틀링** — 브라우저가 숨긴 탭의 타이머를 조인다(1초 → 5분 이상 숨기면
  intensive throttling). F09 Phase 6(브라우저 알림)의 지연이 여기서 나오는데, **타이머를 어떻게
  조절해도 없앨 수 없다.** 서버 푸시는 타이머가 아니라 메시지 구동이라 **제약 자체가 사라진다.**
- **다중 탭 배수** — 탭마다 독립 폴링이라 요청이 탭 수만큼 늘어난다(F09 미결에서 이관, 2026-08-10).
- **완료 인지 지연** — 활성 5초·숨김 30초는 폴링 주기의 하한이다.

> **전환 대상이 처음 예상보다 줄었다.** P04를 부여한 8-03 시점에는 폴링이 세 곳(알림 피드·
> 재감지·생성)이었지만, **F09 Phase 3이 화면 지역 폴링 2개를 이미 걷어냈다.** 지금 남은 상시
> 폴링은 **알림 피드 하나뿐**이다. 규모 추정("중~대")은 그 전에 매긴 값이다.

## 범위

**포함**

- 알림 피드의 상시 폴링을 **서버 푸시로 대체** (백엔드 커넥션 관리 신설 + 프론트 구독)
- 완료 시점의 서버 → 클라이언트 통지 (감지 완료·생성 완료·실패)

**제외**

- **알림의 사용자별 분리** — 인증이 없다(REQ-27). 커넥션이 생겨도 **소유자를 정의할 수 없어**
  "모두의 알림"은 그대로다. F09의 제외 항목을 그대로 승계한다.
- **F09가 만든 화면·데이터 구조 변경** — 벨·팝오버·스낵바·알림 저장소는 그대로 두고 **전달
  경로만** 바꾼다. 화면이 구독하는 대상(`useNotifications`)의 내부가 바뀌는 것이 이상적이다.
- **Phase 6(브라우저 알림) 구현** — F09 소관이다. P04는 그 지연 제약을 없앨 뿐이고,
  **P04가 끝나면 F09 Phase 6의 완료 기준(`___`초)을 다시 볼 수 있다**는 관계만 있다.

## 결정

| 항목 | 결정 | 근거 | 기각한 안 |
|------|------|------|-----------|
| 전환 자체 | **한다** (2026-08-03 승인 조건) | 상시 폴링 승인의 **전제**였다. 스로틀링·다중 탭·지연은 폴링을 어떻게 조율해도 남는다 | 폴링 유지 + 주기 조정 — 세 문제 중 어느 것도 못 없앤다 |
| 우선순위 | **F09 v1 완료 후** (선행 해소: 2026-08-10) | 전환 대상을 F09가 만든다. 먼저 하면 없는 것을 옮기게 된다 | F09와 병행 |
| 프로토콜 (2026-08-26) | **SSE** — `GET /api/notifications/stream` | 알림은 서버→클라 단방향. 브라우저 `EventSource`가 재연결·`Last-Event-ID`를 내장해 끊김 복구를 프로토콜이 준다. 인프라는 중립(Phase 0) | WebSocket — 승인 조건 문구는 "웹소켓"이었으나 양방향이 필요한 시나리오가 없고, 재연결·복구를 프론트가 직접 짜야 한다. heartbeat가 공짜인 것 하나가 이점인데 SSE `: keepalive` 한 줄이면 상쇄된다 |
| 다중 인스턴스 (2026-08-26) | **1태스크 가정, 프로세스 메모리 브로커.** 가정은 코드 주석 + 이 표로 명시 | dev는 ECS 태스크 1개·uvicorn 워커 1개(Dockerfile CMD에 `--workers` 없음). 확장은 아직 계획에 없다 | 스토리지 폴링 기반 브로드캐스트(각 태스크가 알림 저장소를 짧게 폴링) — 서버측 폴링이 남고, 없는 문제를 미리 푸는 비용. **재검토 조건**: desired-count 2 이상 또는 `--workers` 2 이상으로 가는 순간 |
| 프론트 폴링 (2026-08-26) | **완전 제거** — `setInterval` 0건. 끊김 복구는 **재연결 시 `since` 재동기 1회** | 폴백으로 두면 경로 2개를 영구 유지해야 하고, 폴링이 살아 있는 한 스로틀링·다중 탭 문제가 조용히 되돌아온다 | 폴링을 폴백으로 유지 |
| 재동기 방식 | **서버가 `Last-Event-ID`로 재전송** — 이벤트 `id`=`created_at`, 재연결 요청의 헤더를 `list_feed(since=…)`에 넣어 밀린 알림을 스트림 첫머리에 흘린다 | `EventSource`가 자동 재연결마다 헤더를 붙여 주므로 프론트 코드 0줄. 첫 연결(헤더 없음)은 재전송하지 않는다 — 초기 로드는 기존 `GET /api/notifications` 1회가 담당 | 프론트가 `open` 이벤트에서 `listNotifications({since})` 재호출 — 첫 연결과 재연결을 프론트가 구분해야 하고, 그 분기가 테스트 대상이 된다 |
| 이벤트 페이로드 | 알림 1건 + **`unread_count`** 동봉. 읽음 커서 갱신도 `read` 이벤트로 흘린다 | 계약 #27: 미읽음 수는 서버가 센다. 폴링 응답이 실어 오던 값을 이벤트가 대신 실어야 다른 탭에서 읽은 것이 뱃지에 반영된다 | 프론트가 세기 — 커서 공유가 깨진다 |
| 완료 기준 N (2026-08-26) | **완료 → 화면 반영 ≤ 2s** (활성·숨김 탭 동일) | 폴링 하한 5s/30s를 확실히 대체. Phase 0 전달 지연 0.3s에 여유 | 1s — dev 네트워크 편차에 흔들림 / 5s — 활성 탭은 개선이 없다 |

> 나머지는 **아직 결정된 바 없다.** 프로토콜·인프라·재연결 정책은 전부 아래 미결이다 —
> 지어내면 그 틀린 전제가 계획으로 굳는다.

## 미결 질문

- [x] **프로토콜: WebSocket인가 SSE인가.** → **SSE** (2026-08-26, 결정 표). F09 계획서는 둘을 "서버 푸시(SSE/WebSocket)"로 묶어
      제외했고 승인 조건의 문구는 "웹소켓"이었다. 알림은 **서버 → 클라이언트 단방향**이라 SSE로
      충분할 수 있다(재연결·`Last-Event-ID`가 프로토콜에 내장). 양방향이 필요해지는 시나리오가
      있는지 먼저 확인해야 한다.
      → **인프라는 중립이다(Phase 0, 2026-08-18)** — 둘 다 15분 통과·버퍼링 없음. 차이는 heartbeat
      부담뿐: WS는 uvicorn 기본 프로토콜 ping(20s)만으로 살고, SSE는 앱이 `: keepalive`를 직접
      보내야 한다. 그러니 선택은 재연결·폴백 설계 편의로 정한다(아래 폴백 미결과 한 묶음).
- [x] **Cloudflare Tunnel + ECS Fargate에서 장수명 커넥션이 되는가.** → **된다.** (Phase 0 실측
      2026-08-18, 아래 § Phase 0 결과) SSE·WS 모두 15분 무결점, 실시간(drift ~0.3s 상수).
      단 **오리진이 125초 동안 한 바이트도 안 보내면 edge가 끊는다** — heartbeat가 조건이다.
- [x] **인스턴스가 늘면 어떻게 하나.** → **1태스크 가정, 주석으로 명시** (2026-08-26). 지금은 ECS 태스크 1개라 프로세스 메모리에 커넥션을 들고
      있으면 되지만, 2개가 되는 순간 **다른 태스크에 붙은 클라이언트에게 알림이 안 간다.**
      지금 구조로 갈지, 그때 가서 브로드캐스트를 얹을지.
- [x] **폴링을 완전히 걷을지, 폴백으로 남길지.** → **완전 제거 + `Last-Event-ID` 재동기** (2026-08-26). 커넥션이 끊긴 동안의 알림을 어떻게 복구할지와
      한 묶음이다 — F09의 `since` 기반 재동기를 재연결 직후에 한 번 쓰는 방식이 가능해 보이나
      정해진 바 없다.
- [x] **완료 기준에 넣을 수치.** → **N ≤ 2s** (2026-08-26). "지연이 줄었다"는 검증 불가능한 문장이다. 무엇을 몇 초 이내로
      잴지 정해야 한다. F09 Phase 6의 `___`초와 같은 자리이고, 같은 실측 하니스가 필요하다.
      → **절반은 답이 나왔다(Phase 0)**: 인프라 상수는 **idle 컷 125s → heartbeat 30s**(컷의 1/4),
      전달 지연은 **~0.3s**(HKG edge 왕복). 남은 건 "완료 → 화면 반영 N초"의 N — 폴링 5s/숨김 30s의
      하한을 대체하는 값이므로 **N ≤ 2s** 정도가 자연스럽지만 확정은 Phase 1 설계 때 한다.
- [ ] **스트림이 전역 딤(`apiFetch`)을 켜지 않는지 어떻게 재나** — 계약 #26 회귀 케이스 후보인데 `client.js`의
      로딩 카운터가 노출돼 있지 않아 측정 방법이 없다(2026-08-26 `/testgen`). 노출하면 그 자체가 표면 변경. 코드로 쓰지 않았다.
- [x] **숨김 탭에서 `EventSource`가 스로틀링 없이 수신하는가** → **받는다** (2026-08-26 실측, 아래 § 숨김 탭 결과).
      숨김 7.5분 동안 91건 전량 도달, drift 평균 10ms·최대 485ms, **5분 경과(intensive throttling 구간) 이후도 0~4ms**. 브라우저 타이머
      스로틀링은 `setTimeout`/`setInterval` 대상이고 네트워크 이벤트는 아니라고 알려져 있으나 **실측 전**이다.
      Phase 1 착수 전 dev 프론트 콘솔에서 5분 확인(아래 Phase 1 선행 조건). 실패하면 N ≤ 2s의 "숨김 탭 동일"이 깨진다.
- [ ] **① 기준 "N ≤ 2s"에 배포 직후(콜드 태스크)를 포함하는가.** 2026-08-27 실측에서 배포 후 첫 3건만 2.3~3.1s,
      이후 13건은 ≤ 1.3s. 원인 미확인(콜드 태스크의 R2 PUT+LIST 뒤 publish 추정). 정상 상태 기준이면 충족.
- [ ] **`: connected` 선발송을 넣을 것인가.** edge가 첫 바이트까지 헤더를 붙잡아 `EventSource`가 최대 30초
      `CONNECTING`으로 보인다. `event_stream()` 구독 직후 코멘트 1줄이면 즉시 열린다(비용 0, 백엔드 한 줄).

## 작업 단계

> **첫 단계가 실측인 이유** — 프로토콜·구조를 정하기 전에 **인프라가 장수명 커넥션을 통과시키는지**
> 부터 확인해야 한다. 여기서 막히면 나머지 계획이 통째로 무의미해진다. 코드를 먼저 쓰면
> "다 만들고 배포에서 막히는" 순서가 된다.

- [x] **Phase 0 — 인프라 실측 (main 코드 변경 없음 — 프로브는 throwaway 브랜치)** ✅ 2026-08-18
      Cloudflare Tunnel → ECS Fargate 경로에서 WebSocket/SSE 커넥션이 유지되는지 확인한다.
      완료 기준: **dev 환경에서 최소 N분 동안 끊기지 않는 커넥션**으로 서버 → 클라이언트 메시지가
      도달한다. → **15분(N=15) 완주, SSE·WS 모두.** 결과는 아래 § Phase 0 결과.
      ⚠️ 미측정 1건: **브라우저 숨김 탭**에서 스로틀링 없이 수신하는지(노드 클라이언트로는 재현 불가).
      Phase 1 착수 전 dev 프론트 콘솔에서 EventSource로 5분 확인할 것.

- [x] **Phase 1 — 백엔드: 브로커 + SSE 스트림** ✅ 2026-08-27 (P04-01~13 13/13) *(백엔드만, 프론트 무변경 — 기존 폴링과 공존)*
      선행 조건: 숨김 탭 EventSource 5분 실측 → ✅ 2026-08-26 통과(§ 숨김 탭 결과).
      - 프로세스 내 브로커: 구독자별 `asyncio.Queue`. `notification_service.emit()`이 저장 성공 후 publish.
        **emit은 BackgroundTasks threadpool에서 돈다** → `loop.call_soon_threadsafe`로 넘긴다(아래 함정).
      - `GET /api/notifications/stream`: `text/event-stream`, 이벤트 `id`=`created_at`, 페이로드에 `unread_count`
        동봉, `: keepalive` **30s**, `Last-Event-ID` 헤더가 오면 `list_feed(since=)` 결과를 먼저 흘린다.
        `POST /notifications/read`는 `read` 이벤트(`unread_count: 0`)를 publish.
      - 1태스크 가정 주석을 브로커 모듈 상단에 적는다.
      완료 기준: 케이스 ID `P04-01~` 백엔드 테스트 통과(발행→구독 도달 · Last-Event-ID 재전송 · 첫 연결 무재전송 ·
      read 이벤트 · 구독 해제 시 큐 정리 · 저장 실패 시 publish 안 함). 로컬 `curl -N`으로 감지 완료 시 이벤트 도달.
      **dev 배포 후** `curl -N`으로 heartbeat만으로 **≥ 5분 유지**(125s 컷 통과) — 로컬로는 이 항목을 못 잰다.

- [x] **Phase 2 — 프론트: `NotificationProvider` 전달 경로 교체** ✅ 2026-08-27 (P04-20~27 8/8 · 프론트 전체 44/44 · `setInterval` 0건) *(표면 불변)*
      - `EventSource` 구독으로 `setInterval`·`visibilitychange` 감속 로직을 걷어낸다 (`ACTIVE_POLL_MS`/`HIDDEN_POLL_MS` 삭제).
      - 첫 진입 `GET /api/notifications` 1회(30일 기준선)는 그대로. 이벤트 수신은 기존 `poll`의 병합 로직
        (`keyOf` dedup · 누적 · `unread_count` 반영)을 재사용한다 — 데이터 처리는 안 바뀌고 **도착 경로만** 바뀐다.
      - `useNotifications`·`useJobCompletion`·`useNotificationRefresh`의 시그니처·동작 불변. 벨·스낵바·목록 화면 무수정.
      - raw `EventSource`(계약 #26 — `apiFetch` 경유 금지, 전역 딤 없음).
      완료 기준: `P04-` 프론트 케이스 통과(EventSource 생성 1회 · `setInterval` 0건 · 이벤트→상태 반영 · `read` 이벤트→뱃지 0 ·
      중복 이벤트 무시) **+ F09 잔여 케이스(18·25~28, Phase 3·5 전부)·F11 10건 회귀 0건.** `grep -rn setInterval frontend/src` 0건.
      ⚠️ **F09-19·20·21·22·23·24는 폴링 자체를 단언하므로 정의상 깨진다** → P04-20~27로 대체(2026-08-26 `/testgen`에서
      발견·승인). 이 6건은 Phase 2에서 테스트 코드를 지우고 F09 표에 "P04로 대체"를 남긴다. 처음 적은 "F09 11건 회귀 0건"은 틀린 기준이었다.
      ⚠️ 타이머 단언 케이스에 `waitFor` 금지(계약 #25).

- [ ] **Phase 3 — dev 배포 + 실측 + 문서 정리**
      - 백엔드 → 프론트 순 배포(CLAUDE.md 절차. 프론트는 wrangler 수동).
      - 실측 표를 이 계획서에 남긴다: ① 감지/생성 완료 → 벨 뱃지 반영 **≤ 2s**(활성 탭·숨김 탭 각 3회)
        ② 탭 2개에서 요청 수(네트워크 탭: 스트림 1개씩, 폴링 0건) ③ 서버 5분 idle 후 알림 도달(heartbeat 검증)
        ④ 네트워크 끊었다 복구 → 그 사이 알림이 `Last-Event-ID`로 도달.
      - CLAUDE.md 계약 #26·#27의 "폴링" 문구를 스트림 기준으로 정정, `PLAN-F09` Phase 6의 지연 제약 문구 갱신.
      완료 기준: 위 ①~④ 전부 수치와 함께 기록. 확인 뒤 dev 백엔드 `desired 0`.
      → 2026-08-27: 배포 완료, ②③④ CLI 통과, ① 전송 지연은 정상 0.26~1.29s(§ Phase 3 결과). **남은 것**: ①의 벨 뱃지
        활성·숨김 탭 각 3회 + ②의 네트워크 탭(브라우저, 사람) · 계약 #26·#27 문구 정정(완료) · desired 0.

## Phase 0 결과 — 인프라 실측 (2026-08-18)

**방법**: throwaway 브랜치 `feat/P04-phase0-probe`(커밋 `8b50a57`·`e8ac995`, 측정 후 삭제)에
`GET /api/_probe/sse`·`WS /api/_probe/ws`(heartbeat 간격·자동 종료를 쿼리로) + 측정 클라이언트
(도착 간격·drift = 클라 경과 − 서버 경과·종료 원인 JSONL). 이미지 `p04-probe` 태그로 dev에 배포
(태스크 정의 rev 3, `latest`는 안 건드림). 클라이언트는 이 머신(서울) → Cloudflare **HKG** edge.

| # | 모드 | heartbeat | 관측 | 결과 | 수치 |
|---|------|-----------|------|------|------|
| 1 | SSE | 15s | 15분 | ✅ 완주 | 60건, max_gap 15.3s, drift 0.29→0.29s (max 0.60) |
| 2 | WS | 15s + 60s마다 클라→서버 echo | 15분 | ✅ 완주·양방향 정상 | 74건, drift 0.33→0.31s |
| 3·3b | SSE | **없음** | 15분·5분 | ❌ **125.1s(서버 로그)·125.4s(클라)에 끊김** | 2회 재현 |
| 4 | WS | 앱 메시지 없음(uvicorn 20s 프로토콜 ping만) | 15분 | ✅ 완주 | 프로토콜 ping도 "데이터"로 친다 |
| 5 | SSE | 60s | 5분 | ✅ 완주 | drift 0.32→0.32s |
| 6 | SSE | 130s | 5분 | ❌ 첫 tick 전 **125.3s**에 끊김 | 컷은 "마지막 바이트 이후 125s" |

**결론**
- 경로는 장수명 커넥션을 **통과시킨다.** SSE·WS 모두 버퍼링 없음 — cloudflared의 GET-SSE 버퍼링
  보고([cloudflared#1449](https://github.com/cloudflare/cloudflared/issues/1449), Quick Tunnel)는
  named tunnel + 이 구성에서 **재현되지 않았다.**
- **idle 컷 = 125초.** Cloudflare 문서의 Proxy Read Timeout(오리진→edge 무전송 125s → 524)과
  정확히 일치한다([connection-limits](https://developers.cloudflare.com/fundamentals/reference/connection-limits/)).
  60s heartbeat는 살고 130s는 죽는다. → **heartbeat 30s** 권장(컷의 1/4).
- **WS는 uvicorn 기본 `--ws-ping-interval 20`만으로 산다.** 이 값을 끄거나 125s 이상으로 올리면
  같은 컷에 걸린다 — 배포 CMD를 바꿀 때 기억할 것.
- **SSE는 앱 레벨 heartbeat가 필수**(`: keepalive` 코멘트 라인이면 충분).
- 백엔드 `TimeoutMiddleware`(30s)는 `BaseHTTPMiddleware`라 **응답 헤더까지만** 재고 스트림 본문은
  안 끊는다(로컬·dev 모두 확인). WS는 http scope가 아니라 아예 지나지 않는다.
- 우리 터널은 토큰 기반(원격 관리)·ingress `http://localhost:8000` 하나라 cloudflared 오리진
  파라미터는 전부 기본값(`keepAliveTimeout 1m30s`·`tcpKeepAlive 30s`)이다 — 바꾼 게 없다.

### 숨김 탭 결과 — 브라우저 스로틀링 (2026-08-26)

**방법**: 로컬 stdlib SSE 서버(127.0.0.1:8765, 5초마다 서버 시각 전송) + 실제 브라우저(macOS 기본 브라우저)
페이지. 수신 시 `navigator.sendBeacon`으로 `client_ms`·`document.hidden`을 되돌려 drift = 클라 − 서버.
터널 없음 — 재는 것이 인프라가 아니라 **브라우저의 비활성 탭 처리**라서.

| 상태 | 건수 | drift min/avg/max | 유실 |
|------|------|-------------------|------|
| visible | 2 | 1 / 5 / 10 ms | 0 |
| hidden (7.5분) | 91 (seq 2~93) | 0 / 10 / **485** ms | **0** |
| hidden **5분 경과 후** | seq 62~93 | 0~4 ms | 0 |

**결론**: 타이머 스로틀링(1s → 5분 후 1분)이 **`EventSource` 메시지 이벤트에는 적용되지 않는다.**
폴링이 숨김 탭에서 30s+α 늦던 제약은 SSE로 사라진다 — 계획서의 "N ≤ 2s, 활성·숨김 동일"이 성립한다.
단 **이 페이지는 순수 이벤트 핸들러만 있었다** — 앱에서 수신 후 React 상태 갱신이 붙어도 그것은 타이머가
아니라 동기 실행이므로 같은 경로다. 미측정으로 남는 것: 창 최소화가 아닌 **다른 데스크톱/화면 잠금**(측정 안 함).

**측정 도구 결함 1건**: 3번 런에서 `http.client.IncompleteRead`(프록시가 청크 스트림 중간에 끊음)가
`OSError`가 아니라 summary 없이 죽었다. 서버 로그로 125.1s를 읽고, 분류를 고쳐 3b·6으로 재확인했다.
"끊김"은 EOF만이 아니다.

## Phase 3 결과 — dev 실측 (2026-08-27, CLI)

**방법**: 이 머신(서울) → HKG edge → 터널. stdlib SSE 클라이언트 2개(브라우저 UA 필수 — `Python-urllib`은 403),
트리거는 실측용 정크 job(2쪽 샘플)의 `POST /refresh` 16회. 서버 시계가 로컬보다 **+1.60~2.20s** 빠르다
(`uploaded_at` 왕복 5회 교집합) — 아래 ①은 그 편차를 보정한 구간이다.

| # | 항목 | 결과 | 수치 |
|---|------|------|------|
| ① | 완료(`created_at`) → 클라 도착 | 정상 ✅ / 배포 직후 ❌ | 13건 **0.26~1.29s** · 배포 직후(03:17) 첫 3건 **2.3~3.1s** |
| ② | 구독자 2개 동시 수신 | ✅ | 같은 이벤트 12건, 도착 편차 ≤ 0.03s |
| ③ | 5분 idle 후 도달 (heartbeat) | ✅ | 318s 무이벤트 후 0.26~0.87s · keepalive 간격 28.0~30.5s |
| ④ | 끊김 중 발생 → `Last-Event-ID` 복구 | ✅ | 재연결 첫 이벤트 = 놓친 알림 · 살아 있던 클라는 실시간 |

**미측정(브라우저, 사람)**: ①의 벨 뱃지 반영(활성·숨김 탭 각 3회) · ②의 네트워크 탭(스트림 1개씩, 폴링 0).
**부수 발견**: Cloudflare edge가 첫 본문 바이트까지 응답 헤더를 붙잡는다(로컬 4ms vs 터널 30s) → `EventSource`가
최대 30초 `CONNECTING`, 유실 없음. 브라우저에서 벨을 열자 `read` 이벤트가 CLI 클라이언트에 도달(읽음 동기화 통과).

## 검증 계약

> 작성: 2026-08-26 · 스펙: 이 계획서(§ 작업 단계 · § 제약·함정 · § Phase 0 결과) · 검증: `/testrun P04`
> 백엔드 `backend/tests/test_notification_stream.py` · 프론트 `frontend/src/contexts/NotificationContext.stream.test.jsx`
> 테스트가 고정한 인터페이스(계획서에 없던 것): `app.services.notification_broker`(`subscribe()`/`publish()`/`subscriber_count()`),
> `app.routers.notification.event_stream(last_event_id, heartbeat_s)`·`HEARTBEAT_S`, 이벤트명 `notification`/`read`.

| ID | 대상 | 케이스 | 유형 | 근거 | Phase | 결과 |
|----|------|--------|:----:|------|:----:|:----:|
| P04-01 | `emit()` | 저장 성공 후 구독자에 알림 1건 도달 | 정상 | PLAN § 작업 단계 Phase 1 — "저장 성공 후 publish" | 1 | ✅ |
| P04-02 | `emit()` | threadpool(`run_in_threadpool`)에서 호출돼도 구독자가 깨어난다 | 회귀 | PLAN § 제약·함정 — "케이스 하나는 반드시 `run_in_threadpool` 경유로 발행한다" | 1 | ✅ |
| P04-03 | `emit()` | 저장 실패 시 publish 하지 않는다 | 예외 | PLAN § 작업 단계 Phase 1 — "저장 실패 시 publish 안 함" | 1 | ✅ |
| P04-04 | `GET /stream` | 응답이 `text/event-stream` | 정상 | PLAN § 작업 단계 Phase 1 — "`text/event-stream`" | 1 | ✅ |
| P04-05 | `GET /stream` | 이벤트 `id`가 알림의 `created_at` | 정상 | PLAN § 작업 단계 Phase 1 — "이벤트 `id`=`created_at`" | 1 | ✅ |
| P04-06 | `GET /stream` | 이벤트 페이로드에 `unread_count` 동봉 | 정상 | PLAN § 작업 단계 Phase 1 — "페이로드에 `unread_count`" | 1 | ✅ |
| P04-07 | `GET /stream` | `Last-Event-ID` 이후 알림을 스트림 첫머리에 흘린다 | 정상 | PLAN § 작업 단계 Phase 1 — "`Last-Event-ID` 헤더가 오면 `list_feed(since=)` 결과를 먼저 흘린다" | 1 | ✅ |
| P04-08 | `GET /stream` | 헤더 없는 첫 연결은 기존 알림을 재전송하지 않는다 | 불변식 | PLAN § 제약·함정 — "첫 연결에는 재전송하지 않는다." | 1 | ✅ |
| P04-09 | `mark_all_read()` | `read` 이벤트(`unread_count: 0`)가 구독자에 도달 | 정상 | PLAN § 작업 단계 Phase 1 — "`read` 이벤트(`unread_count: 0`)를 publish" | 1 | ✅ |
| P04-10 | `GET /stream` | 알림이 없어도 heartbeat 간격마다 `: keepalive`가 나간다 | 불변식 | PLAN § 작업 단계 Phase 1 — "`: keepalive` **30s**" | 1 | ✅ |
| P04-11 | `HEARTBEAT_S` | 기본 간격 30s | 경계 | PLAN § Phase 0 결과 — "**heartbeat 30s** 권장(컷의 1/4)" | 1 | ✅ |
| P04-12 | 브로커 | 구독 해제 후 구독자 수 0 (큐 정리) | 불변식 | PLAN § 작업 단계 Phase 1 — "구독 해제 시 큐 정리" | 1 | ✅ |
| P04-13 | 브로커 | 구독자 2개에 같은 알림이 모두 도달 | 정상 | PLAN § 작업 단계 Phase 1 — "구독자별 `asyncio.Queue`" | 1 | ✅ |
| P04-20 | `NotificationProvider` | `EventSource`가 `/notifications/stream`으로 1회 생성 | 정상 | PLAN § 작업 단계 Phase 2 — "EventSource 생성 1회" | 2 | ✅ |
| P04-21 | `NotificationProvider` | `setInterval` 0건 | 불변식 | PLAN § 작업 단계 Phase 2 — "`setInterval` 0건" | 2 | ✅ |
| P04-22 | `NotificationProvider` | 첫 진입 GET 1회, `since` 없음 | 정상 | PLAN § 작업 단계 Phase 2 — "첫 진입 `GET /api/notifications` 1회(30일 기준선)는 그대로" | 2 | ✅ |
| P04-23 | `NotificationProvider` | 이벤트 수신 → 목록 누적 + `unreadCount` 반영 | 정상 | PLAN § 작업 단계 Phase 2 — "이벤트→상태 반영" | 2 | ✅ |
| P04-24 | `NotificationProvider` | `read` 이벤트 → `unreadCount` 0 | 정상 | PLAN § 작업 단계 Phase 2 — "`read` 이벤트→뱃지 0" | 2 | ✅ |
| P04-25 | `NotificationProvider` | 같은 `job_id:created_at` 중복 이벤트 무시 | 회귀 | PLAN § 작업 단계 Phase 2 — "중복 이벤트 무시" | 2 | ✅ |
| P04-26 | `useNotifications` | 반환 형태 `{notifications, unreadCount}` 불변 | 불변식 | PLAN § 작업 단계 Phase 2 — "시그니처·동작 불변" | 2 | ✅ |
| P04-27 | `NotificationProvider` | 라우트가 바뀌어도 스트림이 닫히지 않는다 (F09-20 대체) | 불변식 | PLAN § 제약·함정 — "화면이 구독하는 표면을 바꾸지 않는다." | 2 | ✅ |

Phase 3(dev 실측 ①~④)은 코드 테스트가 없다 — 결과는 § 작업 단계 Phase 3에 수치로 적는다.

## 제약·함정

- **알림 쓰기 주체는 백엔드다**(계약 #22·#23, F09 § 결정). 푸시로 바꿔도 **"프론트가 완료를
  판정해 서버에 쓴다"로 돌아가면 안 된다** — B10에서 문제집이 사라진 구조가 그것이다.
- **기준선은 전송 방식과 무관하게 필요하다**(계약 #27). 재연결 직후 밀린 알림을 한꺼번에 받으면
  **앱을 열자마자 30일치 스낵바가 쏟아지던 것과 같은 사고**가 난다.
- **화면이 구독하는 표면을 바꾸지 않는다.** F09가 만든 `useNotifications`·`useJobCompletion`·
  `useNotificationRefresh` 뒤에서 전달 경로만 갈아끼우면 **화면 3곳(벨·목록·작업/편집)을
  다시 손대지 않아도 된다.** 표면까지 바꾸면 F09 검증 47건이 통째로 흔들린다.
- **`apiFetch`를 쓰지 않는다**(계약 #26). 폴링이 사라져도 초기 동기화·재연결 조회가 남는데,
  그 경로에 전역 딤이 켜지면 화면이 번쩍인다.
- **ECS 태스크가 1개라는 사실에 기대는 코드는 그 가정을 주석으로 남긴다.** 확장 시점에 조용히
  깨지는 종류이고, 위 미결(인스턴스 증가)이 답을 못 받은 채 구현되면 특히 그렇다.
- **오리진 무전송 125초면 edge가 스트림을 끊는다**(Phase 0). heartbeat를 "나중에" 붙이면 안 된다 —
  로컬(터널 없음)에서는 heartbeat 없이도 멀쩡해서 **로컬 테스트로는 안 잡힌다.** heartbeat 30s를
  구현의 일부로 넣고, WS라면 uvicorn `--ws-ping-interval`을 끄지 않는다.
- **`emit()`은 asyncio 루프 밖(threadpool)에서 호출된다** — `BackgroundTasks`가 sync 함수를 threadpool에서
  돌리기 때문(`extract.py` 상단 주석). `asyncio.Queue.put_nowait`를 다른 스레드에서 직접 부르면
  **에러 없이 구독자가 깨어나지 않는다**(루프에 wakeup이 안 감). 앱 startup에서 루프를 잡아 두고
  `loop.call_soon_threadsafe`로 넘긴다. 로컬 단위 테스트가 같은 스레드에서 돌면 이 결함이 **안 잡힌다** —
  케이스 하나는 반드시 `run_in_threadpool` 경유로 발행한다.
- **첫 연결에는 재전송하지 않는다.** `Last-Event-ID` 없는 연결에 최근분을 흘리면 앱을 열자마자
  스낵바가 쏟아진다(계약 #27의 사고와 동일). 기준선은 기존 GET 1회가 잡는다.
- **`unread_count`는 이벤트에 실어 보낸다** — 프론트가 세면 안 된다(계약 #27). 이벤트마다
  `list_feed`를 다시 계산하면 파일 나열 비용이 붙는데, 알림 발생 빈도(분당 수 건 이하)에서는 무시할 수준이다.
- **1태스크·1워커 가정.** `desired-count` 또는 `--workers`를 올리면 다른 프로세스의 구독자는 알림을 못 받는다.
  브로커 모듈 주석과 이 계획서 결정 표가 그 가정의 기록이다.
