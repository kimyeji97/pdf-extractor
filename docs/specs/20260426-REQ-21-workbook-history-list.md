# REQ-21 생성된 문제집 이력 목록

| 항목 | 내용 |
|------|------|
| 날짜 | 2026-04-26 |
| 작성자 | kimyeji97 |
| 상태 | confirmed |

---

## 1. 배경·목표

**배경**

생성한 문제집을 나중에 다시 확인·관리할 수 있어야 한다.

**목표 / 달성 기준**

- 생성된 문제집 이력 목록을 조회한다.

---

## 2. Scope

**In-scope**

- `GET /api/workbooks` 이력 목록
- `WorkbookHistoryView` / `WorkbookHistory` 렌더링

**Out-of-scope (non-goal)**

- 재다운로드 (REQ-22)

---

## 3. API·데이터 변경

### API

| Method | Path | 설명 |
|--------|------|------|
| GET | `/api/workbooks` | 문제집 이력 목록 |

### 데이터 모델·스키마

`workbooks/{workbook_id}.json` 스캔 결과.

### 마이그레이션 메모

`workbooks/` 디렉토리 미존재 시 빈 목록(REQ-B01에서 자동 생성 보장).

---

## 4. 테스트 시나리오

| # | 시나리오 | 기대 결과 |
|---|----------|-----------|
| 1 | 문제집 생성 후 이력 조회 | 목록에 표시 |
| 2 | 저장소 비어 있음 | 빈 목록 |

---

## 5. 미결 질문 (Open Questions)

- 없음 (구현 완료). 초기 미노출 버그는 REQ-B01에서 수정.
