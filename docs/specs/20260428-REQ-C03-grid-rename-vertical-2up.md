# REQ-C03 2단 → 세로 2단 명칭 변경

| 항목 | 내용 |
|------|------|
| 날짜 | 2026-04-27 |
| 작성자 | kimyeji97 |
| 상태 | confirmed |

---

## 1. 배경·목표

**배경**

가로 2단(REQ-C04) 추가에 따라 기존 "2단"의 방향을 명확히 구분해야 한다.

**목표 / 달성 기준**

- 기존 `2단`(1행×2열) 명칭을 `세로 2단`으로 변경한다.

---

## 2. Scope

**In-scope**

- `layout_spec.py` / `workbookLayout.js` 키 명칭 변경, `DEFAULT_LAYOUT = "세로 2단"`

**Out-of-scope (non-goal)**

- 가로 2단 추가 (REQ-C04)

---

## 3. API·데이터 변경

### API

`layout` 값 문자열 변경(`2단` → `세로 2단`).

### 데이터 모델·스키마

`LAYOUTS["세로 2단"] = {rows:1, cols:2}`.

### 마이그레이션 메모

기존 이력에 `2단`이 저장돼 있으면 표기 매핑 필요.

---

## 4. 테스트 시나리오

| # | 시나리오 | 기대 결과 |
|---|----------|-----------|
| 1 | 레이아웃 목록 | `세로 2단`으로 표기 |
| 2 | 기본 레이아웃 | `세로 2단` |

---

## 5. 미결 질문 (Open Questions)

- 없음 (구현 완료)
