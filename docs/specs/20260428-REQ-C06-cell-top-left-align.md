# REQ-C06 셀 좌측 상단 정렬

| 항목 | 내용 |
|------|------|
| 날짜 | 2026-04-27 |
| 작성자 | kimyeji97 |
| 상태 | confirmed |

---

## 1. 배경·목표

**배경**

기존 가운데 letterbox(`contain_fit`) 배치는 셀마다 문항 시작 위치가 달라 정돈돼 보이지 않았다.

**목표 / 달성 기준**

- 문항 이미지를 셀 좌측 상단에 고정 배치한다(`contain_fit` → `top_left_fit`).
- 종횡비는 유지한다.

---

## 2. Scope

**In-scope**

- `layout_spec.py` `top_left_fit()` (별칭 `contain_fit` 유지)
- `workbookLayout.js` `topLeftFit()` (별칭 `containFit`)

**Out-of-scope (non-goal)**

- 구분선 (REQ-C05)

---

## 3. API·데이터 변경

### API

없음

### 데이터 모델·스키마

피팅 함수 교체. 기존 호출부 호환 위해 별칭 유지.

### 마이그레이션 메모

없음

---

## 4. 테스트 시나리오

| # | 시나리오 | 기대 결과 |
|---|----------|-----------|
| 1 | 가로가 긴 문항 | 셀 너비 기준 맞춤, 좌상단 고정 |
| 2 | 세로가 긴 문항 | 셀 높이 기준 맞춤, 좌상단 고정 |
| 3 | 미리보기 vs PDF | 동일 정렬 |

---

## 5. 미결 질문 (Open Questions)

- 없음 (구현 완료)
