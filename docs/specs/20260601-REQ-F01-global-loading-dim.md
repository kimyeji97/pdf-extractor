# REQ-F01 전체 화면 로딩 딤

| 항목 | 내용 |
|------|------|
| 날짜 | 2026-06-01 |
| 작성자 | kimyeji97 |
| 상태 | confirmed |

---

## 1. 배경·목표

**배경**

데이터 로딩 중 사용자가 중복 클릭할 수 있고 진행 상태가 불명확했다.

**목표 / 달성 기준**

- API 통신 중 전체 화면에 반투명 딤 + 스피너를 표시한다.
- 병렬 요청이 모두 완료되면 딤을 해제한다(카운터 기반).

---

## 2. Scope

**In-scope**

- `GlobalDim.jsx` 신규
- `client.js` `apiFetch` 래퍼 + `setLoadingCallback`(카운터)
- `App.tsx` 콜백 등록 + `<GlobalDim>` 렌더

**Out-of-scope (non-goal)**

- 이미지 스켈레톤 (REQ-F02)

---

## 3. API·데이터 변경 (UI/UX)

### 동작·상태

전역 `isApiLoading`을 콜백으로 관리(순환 의존 회피). `_activeCount > 0`일 때 딤 표시.

### 컴포넌트

`GlobalDim`(오버레이+스피너), `client.js` 공통 fetch 래퍼.

### 마이그레이션 메모

없음

---

## 4. 테스트 시나리오

| # | 시나리오 | 기대 결과 |
|---|----------|-----------|
| 1 | `GET /api/jobs` 중 | 전체 화면 딤+스피너 |
| 2 | API 완료 | 즉시 해제 |
| 3 | 병렬 요청 2개 이상 | 모두 완료 후 해제 |

---

## 5. 미결 질문 (Open Questions)

- 없음 (구현 완료)
