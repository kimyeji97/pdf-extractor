# REQ-F05 수동 추가 모드 1·3섹션 잠금

| 항목 | 내용 |
|------|------|
| 날짜 | 2026-06-01 |
| 작성자 | kimyeji97 |
| 상태 | confirmed |

---

## 1. 배경·목표

**배경**

수동 추가(드래그) 진행 중 다른 섹션을 조작하면 상태 충돌이 발생할 수 있었다.

**목표 / 달성 기준**

- 수동 추가 모드(`drawMode === true`) 동안 1섹션·3섹션 인터랙션을 비활성화한다(포인터 이벤트 차단 + 반투명 오버레이).
- `[종료]` 클릭 시 즉시 잠금 해제.

---

## 2. Scope

**In-scope**

- `QuestionAnalysisView` 1·3섹션 `panel--locked` 토글
- `.panel--locked` CSS(`pointer-events:none` + `::after` 오버레이)

**Out-of-scope (non-goal)**

- 수동 문항 추가 동작 (REQ-13)

---

## 3. API·데이터 변경 (UI/UX)

### 동작·상태

`drawMode`에 따라 1·3섹션에 `panel--locked` 클래스 적용/해제.

### 컴포넌트

`QuestionAnalysisView`.

### 마이그레이션 메모

없음

---

## 4. 테스트 시나리오

| # | 시나리오 | 기대 결과 |
|---|----------|-----------|
| 1 | `[수동 추가]` 클릭 | 1·3섹션 반투명 오버레이 |
| 2 | 잠금 중 클릭/체크/삭제 | 동작 안 함 |
| 3 | `[종료]` 클릭 | 즉시 잠금 해제, 정상 복원 |

---

## 5. 미결 질문 (Open Questions)

- 없음 (구현 완료)
