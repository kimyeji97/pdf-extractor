2026.07.25 ~ 2026.07.29

문항 분석
. ✅ <버그> 섹션1에 표시되는 문항 수: 수동 문항은 집계 안되어 있음.
. ✅ <추가> 오탐지 문항이 존재하는 경우 1페이지 섹션에서 해당 페이지 하이라이트 표시 (배경색 or 뱃지 추가)
. ✅ <추가> 문제집 삭제 기능이 없음.
. <수정> 문항 목록 섹션은 너비 사이즈에 맞게 문항 사이즈가 같이 늘어 나는 것이 아닌 기준 너비를 넘어서면 nxn 바둑판 배열로 노출되게 (사유: 문항 이미지가 과도하게 커지면 너비 조절하는 의미가 사라짐.)
  ← REQ-D10. REQ-D01(문항 이미지 대형화)과 방향이 반대이므로 "임계 너비 초과 시에만 열 수를 늘린다"로 스펙에 명시할 것
. ⏸ <버그> 서버: 문항 추출시 하단 여백이 통일되지 않음. ← 서버 영역, 이번 작업 제외
. ⏸ <버그> 서버: 문항 추출시 좌/우/상 여백 통일 필요 ← 서버 영역, 이번 작업 제외

문제집 편집
. ✅ <수정> 섹션1에 있는 파일 이름,유형 편집 기능은 '문항 분석' 으로 이동
. ✅ <추가> 섹션4에서 각 문항 별로 이미지 비율 조절 (좌상단 고정) -> 조절한 대로 pdf에 적용되도록

표지 관리
.

생성 이력
. ✅ <버그> [편집] 클릭해서 문제집 편집으로 넘어가면, 이미지가 모두 없음.
. ✅ <추가> 삭제 기능이 없음.
. <수정> 진입시 화면 UI는 문항 분석 과 동일하게 가고, 클릭하면 현재 상태로
.. 클릭시 애니메이션: 오른쪽에 미리보기 화면이 아코디언 액션으로 나타남 + 문제집 목록은 왼쪽 남은 영역으로 조율됨.
  ← REQ-D09
. <추가> 문항 분석 처럼 검색 기능
  ← REQ-F10. 같은 화면이므로 D09와 함께 진행 권장. 서버 검색(list_workbooks) 선행 필요 — 목록이 페이지네이션돼 있어 클라 필터로는 뒷 페이지를 못 찾는다(REQ-P03-03과 같은 이유)

추가 기능
. ⏸ <추가> 로그인, 회원가입 (계정별로 사용 가능) ← REQ-27 별도 계획 필요
. ⏸ <추가> 공유 기능 (문항 분석 파일, 생성된 문제집을 일부/전체 계정에게 공유: 공유 받으면 같이 사용 가능.)
← REQ-28, 로그인 선행

---

## 아래는 완료된 목록 (2026.07.22 기준 전 항목 처리 완료 — 이력 보존용)

2026.07.04

문항 분석
. REQ-F06: 툴바 > 페이지 번호 > 이전/다음 페이지 버튼 클릭시 동작 안함. (페이지 번호를 입력해야만 스크롤 이동됨)
. REQ-F06: 툴바는 페이지 이동 기능을 위한 스크롤 영역에 포함되지 않도록
. <버그> 문항 분석 > 문항 목록 벌크 삭제 안됨
. <수정> 문항 분석 > 페이지 미리보기: 생성 이력 > 문제집 미리보기 처럼 pdf view를 사용
. <버그> 오탐지 의심인 문항은 체크박스 비활성 되어 있어서 삭제 불가능함.

문제집 편집
. <버그> 문제집 편집 > 문항 선택: REQ-B04와 같은 스크롤 문제 존재
. <수정> 문제 이미지 위에 표시하는 라벨 -앞으로 문항 라벨이라고 하자- 데이터 보안 (as-is. {번호}. {문제집이름} p{페이지 번호} -> to-be. {번호}. {문제집이름} p{페이지 번호} {문항 이름})
. <버그> 생성된 PDF를 다운로드 받으면 문항 라벨이 ...로 축약됨. 축약 시키지 마.
. <수정> 맨 우측 미리보기 영역에서 스크롤 좌우가 아닌 상하로 변경

표지 관리
. REQ-D05: 문항 분석의 상세 페이지가 아닌 목록 페이지와 디자인 통일 (2패널 -> 1패널: [+표지 업로드] [표지1] [표지2] [표지3] / +표지 업로드시 모달 사용)

전체
. 속도 및 품질 성능 개선 (REQ-P02) — 상세 spec: `docs/specs/20260629-REQ-P02-performance-improvements.md`

- HIGH: PDF 뷰어 가상화, 페이지 메타 캐시, 목록 API 페이지네이션, 백그라운드 비동기 전환
- MEDIUM: 리스트 memo, 폴링 클린업, API 병렬 호출, 썸네일 중복 읽기, adaptive 감지 조건부 실행
- LOW: 타임아웃, useMemo, DPI 최적화, MUI 번들 사이즈
- REQ-P02 항목 추가: thumbnail request 응답 시간이 왜 오래 걸리는가? (현재 평균 6~10초 -> 모든 통신은 1초 이하로 줄여야 함.)
