# ── 의존성 ────────────────────────────────────────────
node_modules/

# ── 빌드 결과물 ───────────────────────────────────────
dist/
build/
# tsc 증분 빌드 캐시 — 빌드할 때마다 내용이 바뀌어 diff에 계속 뜬다.
# 2026-07-29까지 실수로 추적되고 있었다.
tsconfig.tsbuildinfo

# ── 환경변수 ──────────────────────────────────────────
# .env.local 은 템플릿이므로 커밋 허용
# 실제 값이 들어간 .env 는 커밋 금지
.env
.env.production

# ── Vite 캐시 ─────────────────────────────────────────
.vite/
