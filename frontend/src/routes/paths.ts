export const rootPaths = {
  root: '/',
};

const paths = {
  analysis: '/',
  analysisWork: '/analysis/:jobId',
  editor: '/editor',
  history: '/history',
  format: '/format',
};

export default paths;
